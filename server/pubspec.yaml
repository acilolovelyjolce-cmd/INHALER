name: whimsical_server
description: MongoDB Atlas API + static host for Whimsical Hub
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.17.0
  mongo_dart: ^0.10.7
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.14
